{
  "name": "chatserver",
  "version": "1.0.0",
  "description": "server for chat app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "devsinghindra",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5"
  }
}
